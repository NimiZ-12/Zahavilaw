# דוח בנייה מחדש - SEO / AEO / GEO

_ענף `staging`, אוגוסט 2026. הפרודקשן לא נגוע._

---

## 1. "לא React אלא Next.js" - הבהרה

הלקוח ביקש שהאתר לא ייבנה ב-React. **Next.js בנוי על React ולא קיים
בלעדיו** - אלה לא שתי שפות חלופיות אלא ספרייה והמסגרת שנבנתה מעליה.

מה שגוגל באמת מתקשה איתו הוא אתרים שנבנים **בדפדפן** (client-side
rendering), שבהם הסורק מקבל HTML ריק וצריך להריץ JavaScript. בדיקת הקוד
הראתה שזה **כבר לא המצב** באתר הזה, וגם לא היה קודם:

- כל 11 עמודי האתר ושתי ה-layouts הם Server Components.
- אין ולו `"use client"` אחד תחת `src/app/`.
- ב-build כל העמודים מסומנים `●  (SSG) prerendered as static HTML`.

רק 6 רכיבי ממשק קטנים רצים בדפדפן, וכולם נדרשים לאינטראקטיביות בלבד:
תפריט נייד, טופס יצירת קשר, מתג שפה, נגן וידאו, כפתור וואטסאפ ומעקב
לחיצות. אף אחד מהם לא מכיל תוכן שגוגל צריך לסרוק.

**המסקנה:** לא היה מה "להמיר". מה שכן היה חסר - סכימות, היררכיה, מטא-דאטה
ותמונות מיטביות - תוקן, וזה מה שבאמת משפיע על הדירוג.

---

## 2. מה נבנה

### סכימות (`src/lib/schema.ts`)
קודם: בלוק `LegalService` יחיד, זהה בכל עמוד באתר.
עכשיו: כל עמוד פולט גרף אחד (`@graph`) שהצמתים בו מקושרים ב-`@id` יציב,
כך שמנועי תשובות מזהים את המשרד כישות אחת ולא כאוסף בלוקים מנותקים.

| סוג | מה נוסף |
|---|---|
| `Organization` / `LegalService` | כתובת, קואורדינטות, שעות פעילות, ערוצי קשר, שפות, תחומי ידע |
| `WebSite` | ישות האתר, מקושרת למשרד |
| `Person` | לכל עו"ד - תפקיד, שנת הסמכה, הכשרות, שפות |
| `Service` | לכל אחד מ-6 תחומי העיסוק |
| **`FAQPage`** | **26 שאלות ותשובות שהיו באתר בלי סכימה כלל** |
| `BreadcrumbList` | בכל עמוד פנימי, תואם לפירורי הלחם הגלויים |
| `AboutPage`, `ContactPage`, `JobPosting`, `VideoObject`, `Article` | לפי סוג העמוד |

### עמודים חדשים
- **`/terms`** - תקנון האתר. לא היה קיים, ובלעדיו אי אפשר לקיים את דרישת
  ה-checkbox.
- **`/careers`** - דרושים, מקושר מהפוטר, עם מבנה נתונים מוכן למשרות
  וסכימת `JobPosting`.
- **`/faq`** - עמוד ריכוז שאלות ותשובות.

### עמוד אודות
נבנה מחדש עם היררכיה מלאה (קודם לא היה בו ולו `h2` אחד בגוף העמוד):
על המשרד, המשרד במספרים, עורכי הדין והסמכתם, במה אנחנו מטפלים, הערכים,
ושאלות ותשובות. כרטיסי עורכי הדין מציגים שנת הסמכה, הכשרות ושפות.

### תמונות
| קובץ | לפני | אחרי | חיסכון |
|---|---|---|---|
| clients/jacobs-farm | 223.3 KB | 28.9 KB | 87% |
| clients/agudat-lechem | 123.8 KB | 28.8 KB | 77% |
| team/anat-zahavi | 123.5 KB | 19.6 KB | 84% |
| team/ron-zahavi | 112.5 KB | 14.9 KB | 87% |
| logo | 94.3 KB | 8.6 KB | 91% |
| lobby | 285.5 KB | 192.8 KB | 32% |
| **סה"כ** | **1165 KB** | **413 KB** | **65%** |

כל תמונות הכותרת עברו מ-CSS `background-image` ל-`next/image` - כך הדפדפן
מגלה אותן מוקדם, הן מוגשות ב-AVIF/WebP לפי הדפדפן, ואין קפיצות פריסה.

### שפה
130 מקפים ארוכים (`—`, `–`) הוחלפו במקף רגיל בשני המילונים. עמוד 404
וכפתור הווידאו היו בעברית קשיחה גם בגרסה האנגלית - תוקן.

---

## 3. בדיקות שהלקוח ביקש

| מה נבדק | תוצאה |
|---|---|
| האתר לא בנוי client-side | ✅ כל העמודים HTML סטטי מוכן |
| סכימת FAQ בכל בלוק שאלות ותשובות | ✅ 16/16 |
| חוק הגנת הפרטיות | ✅ עודכן לתיקון 13 + באנר עוגיות |
| חוק הנגשת אתרים | ✅ ניגודיות, מקלדת, הצהרת נגישות |
| אבטחה | ✅ 0 פגיעויות, כותרות אבטחה, CSRF, rate-limit |
| טופס יצירת קשר | ✅ נבדק קצה-לקצה |
| checkbox עם קישורים | ✅ בשתי השפות |

### פירוט `npm run verify:site` (825 בדיקות, 56 עמודים)
- `h1` אחד בדיוק בכל עמוד, בלי דילוג על רמת כותרת
- canonical, hreflang, meta description ו-og:title בכל עמוד
- בלוק JSON-LD אחד תקין בכל עמוד
- `alt` בכל תמונה; אפס מקפים ארוכים בטקסט גלוי
- `/api/contact` דוחה בקשה בלי Origin (403) ובלי הסכמה (422)
- כותרות CSP, HSTS, X-Content-Type-Options, Referrer-Policy

### תיקוני אבטחה
`next` שודרג מ-16.2.7 ל-16.3.3 בעקבות GHSA-6gpp-xcg3-4w24 - עקיפת proxy
באפליקציות App Router עם locale יחיד, כלומר בדיוק המבנה של האתר הזה.
`npm audit` מדווח כעת על **0 פגיעויות**.

### תיקונים שלא היו בתוכנית אבל נמצאו בדרך
1. **לידים נבלעו בשקט** - אם משתני הסביבה של המייל היו חסרים, המבקר קיבל
   "הפנייה נשלחה בהצלחה" והפנייה הלכה לאיבוד בלי שאיש ידע. עכשיו זו שגיאה
   מפורשת בפרודקשן.
2. **שתי אי-דיוקים במדיניות הפרטיות** - המסמך תיאר ספק CRM ששומר פניות
   (לא קיים; הפניות נשלחות במייל) וטען שהאתר לא משתמש בעוגיות פרסום
   (Google Ads מחובר). שניהם תוקנו.
3. **המילון כולו נשלח בכל עמוד** - רכיב באנר העוגיות קיבל את כל המילון
   כ-prop, ולכן כל התוכן הדו-לשוני נשלח בכל טעינת עמוד. `/en/terms` ירד
   מ-124KB ל-68KB.

---

## 4. מה עוד צריך ממך

**לעמוד אודות** (הכי משפיע על סמכותיות):
1. השכלה של כל שותף: מוסד + תואר + שנה
2. מספרי רישיון לשכה
3. הכשרות ותעודות נוספות + קבצי התעודות
4. חברות בוועדות לשכה או אגודות מקצועיות

**כללי:** קישורים לפרופילי לינקדאין / פייסבוק / Google Business
(נכנסים ל-`sameAs` בסכימה ומחזקים את זיהוי המשרד בגוגל).

**לדרושים:** משרות פתוחות, אם יש. המבנה מוכן ויקבל סכימת `JobPosting`.

**להחלטתך:**
- האם להציג את קובצי התעודות עצמם בעמוד אודות, או רק לציין אותן בטקסט
- **Monday CRM לא מחובר בפועל** - הלידים מגיעים במייל בלבד. לחבר?
- ב-4 מתוך 12 קישורי המאמרים תווית המקור לא תואמת את הדומיין
- כמה ניסוחים בתחומי העיסוק גובלים בהבטחת תוצאה ("התוצאה הטובה ביותר
  האפשרית") - מומלץ לרכך, וזה שינוי תוכן שדורש את אישורך

**לפני עלייה לאוויר:** להסיר `SITE_NOINDEX`, לוודא שמשתני
`RESEND_API_KEY` ו-`LEAD_EMAIL_FROM` מוגדרים ב-Vercel (אחרת הטופס יחזיר
שגיאה - במכוון), ולהגיש מחדש את ה-sitemap ב-Search Console.
