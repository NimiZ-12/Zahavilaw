# מצב הפרויקט — אתר משרד עורכי דין זהבי (PROGRESS)

> מסמך המשכיות לעבודה בין sessions. קרא אותו תחילה כדי להמשיך מהנקודה הנוכחית.
> נכתב בעברית כי הלקוח (ron@zahavilaw.com) דובר עברית; הקוד והקומיטים באנגלית.

## ✅ בנייה מחדש ל-SEO/AEO/GEO - עלה לאוויר (30 באוגוסט 2026)

האתר החדש **חי על `www.zahavilaw.com`**. `staging` והפרודקשן זהים.
צ'קליסט העלייה הושלם במלואו - ראה `GO-LIVE.md`.

שכתוב מבני מלא. פירוט מלא ב-`SEO-GUIDELINES.md` (ההנחיות הקבועות) וב-
`REBUILD-REPORT.md` (מה נעשה ומה נבדק). בקצרה:

- **סכימות**: `src/lib/schema.ts` בונה גרף מקושר ב-`@id` בכל עמוד -
  Organization/LegalService, WebSite, Person, Service, BreadcrumbList,
  FAQPage, AboutPage, ContactPage, JobPosting, VideoObject, Article.
- **עמודים חדשים**: `/terms` (תקנון), `/careers` (דרושים), `/faq`.
- **טופס**: checkbox לא מסומן עם קישורים למדיניות ולתקנון; ליד לא נבלע יותר
  בשקט כשהמייל לא מוגדר.
- **תמונות**: הכל WebP דרך `next/image` (חיסכון 65%).
- **שפה**: 130 מקפים ארוכים הוחלפו; אין יותר מחרוזות קשיחות.
- **חוק**: באנר עוגיות שחוסם GA4/Ads עד אישור; מדיניות פרטיות עודכנה
  לתיקון 13 ותוקנו בה שתי אי-דיוקים עובדתיים.
- **אימות**: `npm run verify:site` - 825 בדיקות על 56 עמודים.

**פתוח - ממתין לחומרים מהלקוח:** חברות בוועדות לשכה לעמוד אודות,
פרופילי רשתות חברתיות ל-`sameAs`, ומשרות לעמוד דרושים.

**פתוח - ניקיון ב-Vercel:** ארבעה משתני `MONDAY_*` שאף שורת קוד לא
קוראת (כולל `MONDAY_API_TOKEN`, טוקן חי שכדאי גם לבטל בצד של Monday),
ומשתנה `Google_Private` שלא מזוהה בקוד.

---

## סקירה כללית
אתר תדמית דו-לשוני (עברית RTL / אנגלית LTR) למשרד עורכי דין, Next.js 16 (App
Router) + Tailwind v4. המטרות: מראה מקצועי/יוקרתי, נגישות, SEO גבוה, יצירת קשר
נוחה, ומסירת פניות לתיבת הדוא”ל של המשרד.

## ✅ החלטות נעולות (לא לשנות בלי אישור הלקוח)
- **פלטת צבעים נעולה**: נייבי כהה + זהב + ניטרליים. מוגדרת ב-`src/app/globals.css`
  (משתני `--navy*`, `--gold*`). **אסור לשנות צבעים** בשום שינוי עיצובי.
- **כיוון עיצובי**: "קלאסי ויוקרתי". כותרות ב-**sans-serif מודגש** (Heebo, משקל 800)
  — הלקוח בחר זאת מתוך שתי האופציות שהוצגו לו (serif קלאסי מול sans מודגש כמו
  Herzog). גופן ה-Frank Ruhl Libre הוסר מהפרויקט.
- **שפות**: עברית (ברירת מחדל, RTL) + אנגלית. תוכן במילונים `src/i18n/dictionaries/`.
- **תמונות**: סטוק זמני מ-Unsplash (`src/lib/images.ts`), עד שיגיעו תמונות אמיתיות.

## 🎨 רפרנס עיצוב + מפת דרכים
הלקוח נתן כרפרנס את אתר **Herzog Fox & Neeman** (herzoglaw.com). עקרונות שנשאבו
ומיושמים תוך שמירה על הפלטה שלנו:

| # | משימה | סטטוס |
|---|-------|-------|
| 1 | **Hero** — קופסת נייבי מלאה על תמונת אדריכלות מסך-מלא + מוטיב נקודות זהב | ✅ בוצע |
| 2 | **תחומי עיסוק** — אייקונים גאומטריים דו-גוניים (נייבי+זהב) ייחודיים לכל תחום + רשת נקייה עם קווי הפרדה דקים | ✅ בוצע |
| 3 | **צוות** — סגנון פורטרט, שם בנייבי + תפקיד בזהב + קו מפריד, אולי פאנל ציטוט נייבי | ⬜ הבא בתור |
| 4 | **פאנל ציטוט/אמירה** (נייבי + סימן ציטוט זהב) + מוטיבים דקורטיביים בעמודים הפנימיים | ⬜ |
| 5 | ליטוש ניווט (גלובוס שפה בריבוע נייבי וכו') | ⬜ |

משימה 2 כללה שישה אייקונים ייחודיים (`src/components/Icons.tsx`,
ממופים ב-`src/lib/practice-icons.tsx`): דיני משפחה, מקרקעין, דיני עבודה,
ליטיגציה, מסחרי/חברות, צוואות וירושות — כל אחד מורכב משתי צורות גאומטריות
שטוחות (נייבי + זהב) על רשת משותפת 40×40, מוצגים על תג ברקע `gold-soft`.

## 🏗️ תשתית (הוקם ועובד)
- **פריסה**: Vercel, מחובר ל-GitHub. **הדומיין החי הוא
  `https://www.zahavilaw.com`** (חובר ופעיל). `zahavilaw.vercel.app` עדיין
  עונה כשם משני.
- **ענף עבודה**: `claude/law-firm-website-crm-HASJH` (גם ברירת המחדל ב-repo).
- **מסירת פניות**: הטופס שולח בדוא"ל דרך Resend. משתני הסביבה
  (`RESEND_API_KEY`, `LEAD_EMAIL_FROM`, `LEAD_EMAIL_TO`) צריכים להיות מוגדרים
  ב-Environment Variables של Vercel. **אין סודות ב-repo.**
- **SEO/פרטיות**: המשתנה `SITE_NOINDEX` **אינו מוגדר בפרודקשן**, ולכן האתר
  גלוי לגוגל. הדגל עדיין נתמך בקוד (robots/metadata מכבדים אותו) למקרה
  שיידרש שוב.
- **אבטחה**: security headers + CSP (`next.config.ts`), הגנת CSRF + rate-limit על
  `/api/contact`, honeypot, ולידציה. CSP מתיר תמונות מ-`images.unsplash.com`.

## 🚀 צ'קליסט לפני עליה לאוויר (לטיפול הלקוח)
1. ~~**חיבור דומיין**~~ — ✅ `www.zahavilaw.com` מחובר ומשרת את האתר.
2. ~~**הסרת `SITE_NOINDEX`**~~ — ✅ המשתנה לא היה מוגדר כלל; האתר גלוי לגוגל.
3. ~~**אימות קישורי "מאמרים"**~~ — ✅ נבדק ידנית על-ידי הלקוח באתר החי.
4. ~~**מסירת פניות**~~ — ✅ שני המשתנים מוגדרים; פנייה אמיתית נשלחה מהאתר
   החי והמייל התקבל. (בלעדיהם הטופס מחזיר שגיאה במכוון, כדי שפנייה לא
   תיבלע בשקט.)
5. **Google Analytics** — לחבר Tag ID כשעולים לאוויר (אופציונלי).
6. **תמונות אמיתיות** — להחליף את תמונות הסטוק מ-Unsplash (`src/lib/images.ts`)
   בתמונות אמיתיות של המשרד/הצוות, כשיהיו זמינות.

## 🔁 שיטת עבודה

### סביבות (מאז אוגוסט 2026 — לשינויים גדולים)
| סביבה | ענף | כתובת | תפקיד |
|---|---|---|---|
| **Production** | `claude/law-firm-website-crm-HASJH` | `www.zahavilaw.com` | האתר החי. **לא דוחפים לכאן ישירות** - רק מיזוג מ-`staging` אחרי אישור הלקוח. |
| **Staging** | `staging` | Preview URL של Vercel | כאן מפתחים ובודקים. הלקוח מאשר ויזואלית. |
| **Local** | — | `localhost:3000` | `npm install && npm run dev` בסביבת הסוכן, לבדיקות עומק. |

**זרימת עבודה:** מפתחים ב-`staging` → הלקוח מאשר ב-Preview URL →
מיזוג `staging` לענף הפרודקשן → עולה לאוויר.

> הערה: הרשת של סביבת הסוכן חוסמת כתובות שאינן ב-allowlist, ולכן ה-Preview URL
> אינו נגיש לסוכן. הבדיקות האוטומטיות רצות מול `localhost:3000`; הלקוח בודק
> ויזואלית ב-Preview. Vercel מוסיף אוטומטית `noindex` לפריסות Preview, כך שאין
> סיכון שגוגל יאנדקס את סביבת הבדיקה.

### שוטף
- אני דוחף לענף → Vercel מעדכן את `zahavilaw.vercel.app` תוך ~דקה → הלקוח מרענן ורואה.
- הלקוח עובד על **Mac** (וגם נייד). לא טכני — להסביר בפשטות ולהימנע מהעמסת שלבים.
- **רשת הסביבה**: בסביבות מסוימות חסומה (GitHub/npm בלבד). הלקוח הגדיר allowlist
  לכתובות נדרשות (`zahavilaw.vercel.app`, `images.unsplash.com`,
  ואתר הרפרנס). שינוי מדיניות רשת חל רק על **session חדש**. אם יש גישה — אפשר
  למשוך/לאמת תמונות ולצפות באתר ישירות במקום להסתמך על צילומי מסך.

## 📌 מוסכמות קוד
- `npm run build` ו-`npm run lint` חייבים לעבור לפני push.
- תוכן טקסטואלי רק במילונים (he/en) — שניהם חולקים טיפוס `Dictionary`.
- כתובות תמונות מרוכזות ב-`src/lib/images.ts`.
- לקרוא מדריכי Next 16 ב-`node_modules/next/dist/docs/` (יש breaking changes;
  למשל `middleware` → `proxy`).
