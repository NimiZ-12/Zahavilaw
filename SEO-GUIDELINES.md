# הנחיות עבודה קבועות - אתר זהבי-פריטי ושות׳

> קובץ ההנחיות שהלקוח (ron@zahavilaw.com) ביקש שיישמרו ויכובדו בכל שינוי
> באתר, בכל session. נכתב בעברית כי הלקוח דובר עברית; הקוד והקומיטים באנגלית.
> **לקרוא לפני כל שינוי תוכן או קוד.**

---

## 1. שפה וטיפוגרפיה

- **מקף ארוך אסור.** כל `—` (em-dash, U+2014) ו-`–` (en-dash, U+2013) בתוכן
  גלוי למשתמש מוחלפים ב-`-` רגיל. חל על שני המילונים, על כותרות meta, ועל
  טקסט בתוך רכיבים.
- **שפה אנושית, לא רובוטית.** כותבים כמו שעורך דין מדבר עם לקוח: משפטים
  קצרים, גוף שני, בלי מילות מילוי שיווקיות ובלי ז׳רגון מיותר.
- אין לפתוח פסקה ב"במשרדנו אנו מאמינים ש..." או נוסחאות תבניתיות דומות.

## 2. כללי לשכת עורכי הדין (מחייב משפטית)

אסור בהחלט בכל טקסט באתר:
- **מחירים** או טווחי מחירים, "ייעוץ ראשוני חינם", הנחות.
- **סופרלטיבים**: "הטוב ביותר", "המוביל", "מספר 1", "המומחה הגדול".
- **הבטחות תוצאה**: "מובטח", "נצליח", "תזכו ב...".
- **הכפשת עמיתים** או השוואה למשרדים אחרים.

מותר ורצוי: תיאור עובדתי של ניסיון, תחומי עיסוק, פסקי דין שפורסמו, והסמכות.

## 3. היררכיית כותרות ומבנה

- **`<h1>` אחד בדיוק בכל עמוד**, ותמיד הכותרת הראשית של התוכן.
- `h2` למקטעים, `h3` לתתי-מקטעים. **אין לדלג רמה** (h1 → h3 אסור).
- הפוטר והניווט לא פולטים `h2` - הם לא חלק מהיררכיית התוכן.
- שימוש באלמנטים סמנטיים: `<section>`, `<article>`, `<nav aria-label>`,
  `<main>`, `<aside>`.

## 4. סכימות (Schema.org) - הבסיס ל-SEO/AEO/GEO

כל עמוד חייב סכימה מתאימה לסוגו. הגרף מקושר ב-`@id` דרך `src/lib/schema.ts`
כדי שמנועי תשובות יזהו את המשרד כישות אחת:

| סוג עמוד | סכימה נדרשת |
|---|---|
| כל עמוד | `Organization`/`LegalService` + `WebSite` + `BreadcrumbList` |
| תחום עיסוק | `Service` + **`FAQPage`** |
| שאלות ותשובות | `FAQPage` |
| עמוד עו"ד | `Person` / `Attorney` |
| אודות | `AboutPage` |
| יצירת קשר | `ContactPage` |
| דרושים | `JobPosting` |
| וידאו | `VideoObject` |
| פסק דין / מאמר | `Article` |

**כלל:** כל בלוק שאלות ותשובות באתר, בכל מקום, חייב `FAQPage` schema.

## 5. תמונות

- **WebP בעדיפות ראשונה** (AVIF כשאפשר). אין להעלות JPG/PNG כבדים.
- כל תמונה עוברת דרך `next/image` עם `alt` תיאורי אמיתי - לא `alt=""`
  אלא אם היא דקורטיבית לחלוטין.
- אין להשתמש ב-CSS `background-image` לתמונות תוכן: זה עוקף את מיטוב
  התמונות של Next והתמונה לא נספרת לטובת SEO.
- להקטין לפני העלאה. `scripts/optimize-images.mjs` עושה את זה.
- **Next 16**: `priority` הוצא משימוש - להשתמש ב-`preload`.
  `images.qualities` חייב להיות מוגדר ב-`next.config.ts`.

## 6. עיצוב - נעול, לא לשנות

- **פלטה**: נייבי `#1a2336` + זהב `#b08433` + ניטרליים. מוגדרת ב-
  `src/app/globals.css`. **אסור לשנות את הערכים.**
- **גופן**: Heebo. כותרות במשקל 800.
- **חריג נגישות מותר**: `--gold` על רקע לבן נותן ניגודיות 3.39:1 ונכשל
  ב-WCAG AA לטקסט קטן. לכן זהב מיועד לכותרות גדולות ולאלמנטים דקורטיביים;
  טקסט קטן על לבן משתמש ב-navy. זו החלפת *שימוש*, לא שינוי פלטה.

## 7. תוכן דו-לשוני

- כל טקסט גלוי נמצא **רק** ב-`src/i18n/dictionaries/he.ts` ו-`en.ts`.
  אין מחרוזות קשיחות ברכיבים או בעמודים.
- שני המילונים חולקים את הטיפוס `Dictionary` - שינוי באחד מחייב את השני,
  אחרת ה-build נכשל.
- עברית = RTL וברירת מחדל, אנגלית = LTR.

## 8. כתובות ו-SEO טכני

- **אסור לשנות כתובות קיימות.** בוצעה מיגרציית SEO מלאה מוורדפרס עם
  30 הפניות 301 ב-`next.config.ts`. שינוי כתובת שובר את הדירוג בגוגל.
- כל עמוד חדש נוסף ל-`sitemap.ts` עם `alternates.languages` (hreflang).
- כל עמוד: `generateMetadata` עם title, description, canonical, hreflang,
  ו-openGraph משלו.
- `llms.txt` נוצר דינמית מהמילונים - לא לכתוב אותו ידנית.

## 9. עמידה בחוק הישראלי

- **נגישות**: תקנות שוויון זכויות לאנשים עם מוגבלות, ת"י 5568 / WCAG 2.0 AA.
  הצהרת נגישות ב-`/accessibility` עם פרטי רכז נגישות.
- **פרטיות**: חוק הגנת הפרטיות כולל **תיקון 13** (בתוקף אוגוסט 2025).
  מדיניות פרטיות ב-`/privacy`, תקנון ב-`/terms`.
- **טפסים**: בכל טופס יצירת קשר חובה checkbox **לא מסומן כברירת מחדל**
  עם הטקסט "אני מאשר/ת את מדיניות הפרטיות ותקנון האתר", כאשר
  "מדיניות פרטיות" מקושר ל-`/privacy` ו"תקנון האתר" מקושר ל-`/terms`.
  הוולידציה נאכפת גם בשרת, לא רק בדפדפן.
- **עוגיות**: GA4 ו-Google Ads נטענים רק לאחר הסכמה מפורשת.

## 10. תהליך עבודה

- מפתחים ב-`staging` בלבד. הפרודקשן (`claude/law-firm-website-crm-HASJH`)
  הוא האתר החי - **אין לדחוף או למזג אליו ללא אישור בכתב מהלקוח**.
- `npm run build` ו-`npm run lint` חייבים לעבור לפני כל push.
- **תוכן חדש שמופיע באתר מוצג ללקוח לאישור לפני שהוא נכנס.**
- הסוכן לא יכול לגשת ל-Preview URL של Vercel (חסימת רשת). הבדיקות
  האוטומטיות רצות מול `localhost:3000`; הלקוח בודק ויזואלית ב-Preview.
